```
右值引用：
1、通过&&获得右值引用
2、右值表达式表示对象的值
3、可以将右值引用绑定到要求转换的表达式、字面常量或是返回右值的表达式，但不能将一个右值引用直接绑定到左值上
4、右值要么是字面常量
左值引用：
1、通过&获得左值引用
2、左值表达式表达对象的身份
3、不能将左值引用绑定到要求转换的表达式、字面常量或是返回右值的表达式上，但可以将一个const的左值引用或者一个右值引用绑定到这类表达式上
4、左值有持久的状态

```