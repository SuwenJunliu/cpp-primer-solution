```
replace(beg, end, old_val, new_val)
将beg和end之间的元素old_val用new_val替换

repalce_if(beg, end, pred, new_val)
将beg和end之间满足pred条件的元素用new_val替换

replace_copy(beg, end, dest, old_val, new_val)
将beg和end之间的元素old_val拷贝到dest里然后用new_val替换

replace_copy_if(beg, end, dest, pred, new_val)
将beg和end之间满足pred条件的元素拷贝到dest然后用new_val替换

```